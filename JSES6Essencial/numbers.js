const myNumber = 12.4032;

// Transformar numero em string
const textNumber = myNumber.toString();

// Retorna número com um número determinado de casas decimais
const decimalNumber = myNumber.toFixed(2);

// Transforma em float
const floatNum = parseFloat(myNumber);

// Transforma em int 
const intNum = parseInt(myNumber);